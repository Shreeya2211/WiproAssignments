<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="
         http://www.springframework.org/schema/beans
         https://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/aop
         https://www.springframework.org/schema/aop/spring-aop.xsd">

    <!-- Bean for ShoppingService -->
    <bean id="shoppingService" class="springex8.ShoppingService"/>

    <!-- Bean for LoggingAspect -->
    <bean id="loggingAspect" class="springex8.LoggingAspect"/>

    <!-- AOP Configuration -->
    <aop:config>
        <aop:aspect ref="loggingAspect">

            <!-- Pointcut: All methods of ShoppingService -->
            <aop:pointcut id="shoppingMethods"
                          expression="execution(* springex8.ShoppingService.*(..))"/>

            <!-- Before Advice -->
            <aop:before pointcut-ref="shoppingMethods" method="logBefore"/>

            <!-- After Returning Advice -->
            <aop:after-returning pointcut-ref="shoppingMethods" method="logAfterReturning"/>

            <!-- After Throwing Advice -->
            <aop:after-throwing pointcut-ref="shoppingMethods" method="logAfterThrowing"/>

            <!-- Around Advice -->
            <aop:around pointcut-ref="shoppingMethods" method="logAround"/>
        </aop:aspect>
    </aop:config>

</beans>
